
export interface IResult {
    gameId: number;
    homeScore: number;
    awayScore: number;
}

export class Picks implements IResult {

    gameId: number;
    homeScore: number;
    awayScore: number;

    constructor() { }
}


export class Resuts implements IResult {

    gameId: number;
    homeScore: number;
    awayScore: number;

    constructor() { }
}
